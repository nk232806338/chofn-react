var React = require('react');
var Pagination = require('../../../component/pagination/pagination-base');
var PaginationExample = React.createClass({
  render() {
    return (
      <div>
        <Pagination />
      </div>
    );
  }
});

module.exports = PaginationExample;